export enum Pages {
    posts = 'posts',
    notes = 'notes',
    users = 'users',
    active = 'active'
}

export enum Filters {
    popular = 'popular',
    favorite = 'favorite',
    new = 'new',
    active = 'active',
}